/**
 * [Admin Infrastructure: Next.js Configuration]
 * 
 * [Description] Next.js 프레임워크의 동작 방식을 정의하는 프로젝트 공식 설정 파일입니다.
 * 
 * [Design Intent]
 * - [Strategy] 빌드 타임 환경 변수 및 최적화 설정을 관리하는 최상위 인프라 계층입니다.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
